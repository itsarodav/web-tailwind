/** @type {import('tailwindcss').Config} */
module.exports = {
    // Tailwind v4 recomienda configurar tokens en CSS con @theme (CSS-first).
    // Este config se deja mínimo por compatibilidad / evidenciar configuración.
    theme: { extend: {} },
    plugins: [],
};